package main

import (
	"fmt"
)

func main() {
	//var arr = []int{-1, -2, -3, -4, -5, -6}
	var arr = []int{-2, -3, 4, -1, -2, 1, 5, -3}
	//var arr = [10]int{1, 2, 0, 2, 0, 1, 1, 1, 0, 0}
	fmt.Println("The given array is")
	fmt.Println(arr)

	// function call to reverse an array
	//a.ReverseArray(arr)

	// function to find the minimum and maximium element in an array
	//array.MinMaxElement(arr)

	//function to find the kth smallest element given array elements are distinct
	//a.KSmallestElement(arr)

	//sort an array with 0, 1, 2 without any sorting algo
	//a.SortZOT(arr)

	//Move all negative numbers to beginning and positive to end
	//a.Move(arr)

	//union and intersection of two sorted arrays
	// var arr1 = [4]int{5, 6, 7, 8}
	// a.UnionAndIntersection(arr, arr1)

	//cyclically rotate an array by one
	//a.CycRotate(arr)

	//Kadane's Algorithm -Find the contiguous sub-array(containing at least one number) which has the maximum sum and return its sum.
	//a.KadanesAlgo(arr)

	//Minimize the height of an array
	// a.MinHeight(arr)

	//Minimum number of jumps
	//a.MinJumps(arr)

}
